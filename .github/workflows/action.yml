name: Must run on custom runner

#on: push

on:
 workflow_dispatch:

jobs:
  init:
    runs-on: self-hosted
    steps:
      - run: echo 'hello cloud gurus'
      - run: echo $HOSTNAME
      - run: pwd
      - run: bash /runners/custom-runners/mostrar.sh
      - run: cd /runners/custom-runners && bash ./mostrar.sh
      - run: pwd
